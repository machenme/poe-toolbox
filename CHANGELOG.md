# 更新日志

## v0.2.1 (2026-09-16)

### 新功能
- 「词缀上色」新增**「导出词缀补丁」**：把当前上色方案打包成独立 zip 补丁（词缀文本变更与颜色定义一起带走），对方不装本工具，在「特效补丁」页选择这个 zip 即可应用。
- 「特效补丁」页：内置补丁与自定义补丁包可一键**导出为 zip 分发**；底部选中的待应用补丁包会作为条目显示并默认勾选。**「内置词缀修改补丁」从第三方列表移入内置补丁区**（末尾，与引擎内置特效补丁并排，同样显示已启用 / 未启用状态、可导出）：勾选即参与启用 / 还原 / 卸载，且固定排在所有其他补丁之后执行——「先打完其他补丁，最后词缀修改」的顺序由列表位置直接体现；从未用过词缀上色时该行会置灰并指路。
- 「词缀上色」**列表能显示第三方配色补丁已经上的颜色**：以前列表用剥离了标签的纯文本重建预览，别人补丁打的颜色一律看不见（打开 filesrc 检查器也只会显示成黑字）。现在连上游戏时会读一次 `uisettings.xml` 里的颜色定义（游戏原生 `r,g,b` 与第三方常用的 `a,r,g,b` 四段都认），列表按文件里真实的颜色标签渲染——哪几个字着色、什么颜色，和游戏内一致。你自己的方案优先：**你指派过颜色的词缀显示你的颜色，没管过的显示它本来的颜色**，两者可以同屏共存。外来颜色只用于显示，不会混进你的颜色方案，也不会凭空生成一堆色阶。
- 「词缀上色」**有颜色的词缀自动排到列表最前面**：打开页面或搜索之后，已经上了色的词缀（自己方案指派的、文件里已有颜色的）都在最上面，不必在约两万条里翻找。判定精确到**每一条**词缀——同一文件里只有真正着色的那几条优先，不会被同文件的大量未着色条目淹没。
- 「词缀上色」支持**行级上色**：同一条词缀常有「提高 / 降低」两行变体（游戏按实际数值只显示其中一行），以前一上色就是一对全上。现在检查器「游戏内当前文本」逐行展示，每行可**「只上此行」**——提高和降低各上各的颜色；已单独上色的行可单独撤销。指派按行记录进方案，旧方案文件读回不受影响。
- 「词缀上色」**界面调整**：「一键上色」移到顶部「方案」选择旁；顶部新增**「恢复默认」**——回到刚打开游戏数据的状态（清除应用后模拟预览，列表按游戏文件当前的真实内容显示，不写盘、不动方案）；检查器删掉整条「上色 / 移除颜色」按钮与「当前生效颜色」色块，上色用颜色下拉挪到「游戏内当前文本」旁，逐行「只上此行」更顺手；「负向色阶」暂从界面隐藏（功能保留）。
- 「修改地图标签」新增**「恢复到上一次改动前」**（在「写入设置」下）：第一次点「写入地图标签」时自动把当时的 30 个地图文件（15 / 16 个数字 DDS 及各自的低 mip）原样存一份存档，之后随时可一键撤回——**连续调整字号 / 颜色 / 偏移多少轮，恢复都回到第一次改动之前**，而不是回到上一次微调之前。恢复后这份存档即失效，按钮转为不可用并提示「还没有可撤回的改动」；存档位置 `Bundles2\backup\map-numbers\`，随游戏目录走。只动这一组文件，词缀上色、特效补丁等其他模块的修改不受影响。
- 「词缀上色」**颜色修改里能读到游戏里已有的颜色**了：窗口下方新增「游戏里已经在用的颜色」区，列出当前词缀文件真的引用到的那些颜色（第三方补丁带来的 `AT1/DA1/DF1/LUK1/SP1…` 与游戏自带的 `fire/cold/unique…` 都算），每项显示 id、HEX 与游戏文件里的原始写法（`A,R,G,B`）。不会把客户端里 300 多个颜色定义全倒出来——只列被引用的。点「照抄这个颜色」会按它的 RGB 生成**一个属于你自己的新颜色**（id 自动避开同名），之后指派给哪条词缀，哪条就换成你的颜色。**同名是被禁止的**：和对方的颜色重名等于接管那套配色的全部引用（几百条一起变），保存时会拦下并说明原因。
- 「词缀上色」**能改掉第三方补丁已经上好的颜色**：客户端里已经带着别人配色的词缀（列表里能看到对方的颜色），只要你在「颜色修改」里给它**手动指派**一个颜色，应用时会先把这一条上的外来颜色标签**彻底清空**，再打上你自己的颜色——同一文件里其余词缀的第三方配色一条都不动（改 1 条、其余 99 条保持原样）。关键词规则命中的不在此列：规则一命中往往就是几百条，不会替你把别人的配色批量擦掉。
- 「修改地图标签」**写入前会核对地图文件是否被其他补丁改过**：第一次写入前比对当前内容与游戏原版，若不一致（第三方补丁换过地图底图 / 数字）会列出文件名并二次确认，明确告知继续写入将覆盖对方的内容。已存在存档时跳过这项核对（那时内容本就该是本工具上次写进去的）。

### 变更
- 「词缀上色」的**「卸载 / 还原」只撤自己的颜色，不再回到游戏原版**：以前补丁里备份的是原版索引里的文件，卸载词缀补丁会把第三方配色补丁一并抹掉（只想撤自己的上色，却连累别人整套配色）。现在卸载目标是「应用前那一刻的内容去掉本方案的标签」，第三方配色原样留下；同时应用与导出不再去打开一次原版索引基线（原先约 10~30 秒），快得多。想回到无补丁的干净状态请用「恢复游戏原版」。
- **「恢复默认地图设置」改为「恢复到上一次改动前」**：原实现从原版索引基线把地图文件指回游戏原版，会把那时客户端上第三方的改动一并抹掉。现在改为写回第一次改动前存下的内容——用户改完想撤，该回到的是动手之前那一刻（哪怕那时客户端已经被别的补丁改过），而不是原版。功能随之从「只重定向索引记录」变为「内容写回」，因此不再依赖基线。

### 修复
- 修复「特效补丁」页无视勾选状态执行补丁的问题：底部输入框里选过的补丁文件会**无条件**参与「启用 / 卸载」等操作，取消它的勾选形同虚设；卸载一个补丁后，「待应用」条目还会以勾选状态复活，下一次点「启用」会把刚卸载的补丁**悄悄打回游戏**（实测踩坑：只想启用词缀修改补丁，汉化整包补丁却被重新应用，随后词缀补丁因游戏内容变了而冲突失败，还让人误以为卸载没生效）。现在：输入框文件只有在列表里没有对应条目（刚选完文件）或对应条目被勾选时才参与执行，勾选状态说了算；卸载完成后会清空指向刚卸载补丁的输入框，杜绝「待应用复活」；执行结果总结会**点名失败的补丁**，不再只报「成功 X 个、失败 Y 个」。
- 「词缀上色」生成的补丁在游戏文件变化后（应用 / 卸载其他补丁、换汉化、游戏更新）从「特效补丁」页启用会因内容不一致而冲突中止，现在中止时会附指引：到「词缀上色」页重新点「应用词缀修改」生成匹配当前游戏内容的补丁。
- 修复「词缀上色」反复报 `Could not find a part of the path …PATCHED\affix-workbench_v12.bundle.bin`、重连也修不好的问题（本次追到了根因）：「恢复默认游戏数据」（启动器文件校验、工具内还原）会删除已打补丁的文件；若「原版索引基线」是在补丁应用状态下创建或刷新的，恢复出来的索引仍指向这些已被删除的文件，而自动修复会照着这份被污染的基线把文件「修」回同一个丢失文件——每次都报修复成功，重连却依旧失败，永不收敛。现在：补丁应用状态下不再把当前索引保存为「原版基线」（宁可不建，也不能建脏的），悬空状态下也不刷新基线；自动修复发现基线同样指向丢失文件时不再假装修复，明确提示改用启动器的「验证/修复游戏文件」；「恢复游戏原版」在没有基线时给出中文说明并中止，不再抛英文异常，恢复完成后还会自动自检一遍；价格标签页「还原游戏数据」后清理无引用的补丁文件并自检；「词缀上色」连接后补丁文件再被外部删除时，点「应用」会自动修复索引并重新连接，提示再按一次「应用」即可，不再把英文报错甩出来。
- 修复索引里空的补丁 bundle 记录永不清理、随每次应用 / 还原不断堆积的问题（上游库笔误：腾空 bundle 时数组移除的返回值被丢弃，索引从未真正缩短——物理文件删了，空记录却永久留在索引里，这也是此前版本号一路涨到 v12 的原因之一）。修复后腾空的补丁 bundle 会真正从索引移除；已经堆积的空记录会在下一次保存索引时自动清干净。

## v0.2.0 (2026-09-16)

### 新功能
- 新增「词缀上色」模块（POE2）：浏览、搜索客户端全部词缀描述文件（约 1.9 万条简中词缀，按「装备词缀 / 地图与图鉴 / 掉落容器与遗物 / 技能与天赋 / 其他」分组），给掉落收益词缀与高危词缀自定义上色——颜色标签名与 RGB 全部可配置，支持手动逐条指派与关键词规则组批量命中（手动指派优先）。一键生成补丁并应用（含 uisettings.xml 颜色定义注入），补丁内带原版备份可随时恢复；重复应用自动递增补丁版本，与特效补丁共用执行通道互斥执行。上色方案（颜色 + 规则 + 指派）可保存、导入、导出为 JSON 分享。
- 「词缀上色」以游戏内实况文件为底稿、只重写本方案拥有的颜色标签：已应用其他词缀配色补丁（如 efarm 系列实例补丁）时标签原样保留，可分层叠加；游戏文件被外部改动后应用会被拒绝并提示重新连接。

### 修复
- 修复打开「词缀上色」页面时自动执行一次「等级色阶重建」的问题：初始化下拉框误触了用户操作事件，导致每次打开页面都会把已自定义的档位颜色覆盖回默认渐变并写入方案。现在只有用户手动改档位数才会重建。
- 修复索引引用的补丁 bundle 文件被外部删除后（如客户端校验清理了 `PATCHED` 目录）一切读取受影响文件的操作都报 `Could not find file` 的问题：词缀上色连接时会自动发现这种悬空引用，从原始索引基线找回每个文件的原始位置并写回，随后清理失效的补丁记录；游戏本体因同一原因加载失败也会被顺带修好。修复过程写入界面日志。本次更新进一步覆盖 `PATCHED` 目录被整体删除的形态（此时报的是 `Could not find a part of the path`），并修正了此前「PATCHED 目录不存在就跳过检查」的错误预检；「特效补丁」还原整包替换型补丁后也会就地自检一次，防止还原出的旧索引快照引用已被删除的补丁文件。
- 修复卸载「整包替换型补丁」（zip 内自带 `_.index.bin` 的补丁包）时索引被旧快照覆盖、其他补丁随之一并损坏的问题：卸载这类补丁前会弹窗说明——它修改了游戏索引，卸载会把索引换回快照版本，其他所有已启用的补丁都会失效，因此将被连带全部卸载（不含索引的普通补丁仍可单独卸载）；执行顺序改为先还原索引快照、再逐个卸载其余补丁，保证过程中引用的补丁文件都还存在，最终索引干净一致。
- 修复外部工具改过的游戏文件丢掉 BOM 后无法识别的问题：这类文件内容仍是 UTF-16LE，只是文件头少了两个字节，此前会被当成 UTF-8 读取，`metadata/ui/uisettings.xml` 因此找不到根元素结束标签，「应用词缀修改」会直接报错失败。现在会按字节特征自动认出这种无 BOM 的 UTF-16LE，并且写回时保持原样、不擅自加上 BOM。词缀描述文件与特效补丁的文本处理同样受益。
- 修复补丁写入过程中回读"正在写入的 Bundle"内文件必然失败的问题：Bundle 元数据在 `Save` 之前不反映已追加的内容，此前一旦补丁在写入通道内二次读取自己刚写过的文件（如 v2 地面燃烧补丁对 miscanimated.datc64 的第二次指针重定向），就会抛 `offset must be less than or equal to '0'` 并整体回滚。现在未落盘的待写内容直接从写入缓冲读取。

### 改进
- 主界面状态栏**最左下角新增「恢复游戏原版」兜底入口**：与「特效补丁」页的恢复是同一个功能（用备份的原版索引整体替换当前索引，并删除所有补丁新增的文件，含第三方补丁），红色样式、点击后二次确认；执行流水直接展开在全局输出日志里。无论当前在哪个模块、把游戏改成了什么状态，都能从主界面一步回到原版。日常卸载单个补丁仍请到「特效补丁」页。
- 「特效补丁」页面会自动显示**已经打过哪些补丁并默认勾选**：打开模块时直接列出每个内置补丁的启用状态（已启用 / 未启用），已启用的自动勾上，想还原直接点「恢复游戏原版」，不用先猜打过什么。状态来自游戏目录里的一份小记录 `Bundles2\backup\applied-patches.json`——补丁应用 / 还原 / 卸载时由引擎顺手维护，所以打开页面是秒开，不必再为了看状态去打开几 GB 的索引。记录只用于展示，不参与还原（还原依旧靠索引本身与原版基线）；升级前就打过补丁的客户端，首次打开会核对一次索引把记录补齐，之后都只读记录。自定义补丁与整包替换型补丁也会记在这份记录里，列在补丁列表下方。「刷新补丁状态」改为真正打开索引核对一遍并纠正记录，会覆盖手动勾选；「卸载已勾选补丁」移入「高级维护」（它删文件、不可逆），主操作区只留「启用」与「恢复游戏原版」。
- 界面与模块改名：侧栏分组「通用」改为「菜单」，且菜单宽度随最长模块名自适应；「术语翻译」改为「攻略翻译」，「词缀工作台」改为「词缀上色」；「词缀上色」里「连接游戏」改为「打开游戏文件」、「颜色维护」改为「颜色修改」。
- 「词缀上色」新增**「一键上色」**：给所有带档位阶梯的装备词缀（约 600 个）一次性批量上色，应用后每个词缀按数值拆成 **T1~T4 四档**并全部上色，不必逐个勾选。若方案里还没有等级色阶，会**自动创建默认的一套绿色阶 `Tier1~Tier4`**（T1 荧光黄绿最醒目，逐档大幅变暗、透明度递减）；想换成别的颜色随时去「颜色修改」改，或用「新增下一级」自建色阶——一键功能支持任意色阶，不限于绿色。真实规模实测：主库命中 156 个词缀、文件体积 +0.2%、耗时约 0.7 秒（含首次构建档位索引 0.55 秒）。
- 「词缀上色」新增**分档实时预览**：选中色阶（或选中某条词缀）后，检查器下方会立刻按这套颜色渲染出 T1~T4 各档的实际效果，并用选中词缀的真实档位数值填充（如 `+200 生命上限（200-214）`），最低的染色档会注明"更低的档不上色"。换色阶即时刷新。
- 「词缀上色」装备词缀支持**按 tier 阶梯分档上色**（只染最高的几档——跟随等级色阶档数，默认 T1~T4，更低的档不上色）：从 `mods` 表取该词缀的普通词缀档位区间（同一词缀常有多条并行的线——单手武器、双手、混合词缀等，取档数最多的那条作基准），把描述文件里原来那一行**按档拆成多行**，每档一行、只染数值并给不同深浅的颜色（T1 最亮、越低档越暗），最高档用 `|#` 兜住更高数值，未染色的低档合并成一行 `1|…` 兜底且不上色。目前 612 个词缀有可用的 tier 阶梯。整套只在首次用到时构建索引（约 1 秒），不拖慢连接。
- 「词缀上色」新增**数值分档上色**（装备词缀走这套，其他词缀照旧整条上色）：勾选词缀后点「按数值分档」，工具会按每行行首的数值区间自动分配色阶——区间越高颜色越亮；对「没有区间但带正负号」的词缀（如"所有法术技能等级 {0:+d}"）会先**拆成"提高 / 降低"两行**再分别染色（提高用正向色阶首档，降低用负向色阶，写法与实例补丁一致：`1|# …` 与 `#|-1 … negate 1`）。染色只作用于**数值占位符**（如 `{0}%`），词缀名保持游戏默认色。色阶不用新建概念：同前缀的颜色自动成组（`AT1/AT2/AT3` → 「AT（3 级）」），配合颜色修改里的「新增下一级」即可建好。另外，剥离现在也认**无闭合标签**（`<X>{{…}}`），所以外部补丁打过的配色可以被本工具接管重涂。
- 「词缀上色」组合型词缀会在列表里标注说明：有些词缀的文本本身只有占位符（如风暴前后缀那条就是 `{0} {1}`），实际内容由游戏运行时把前后缀拼进去，离线预览无法还原。这类条目现在右侧会显示「由前后缀组合，游戏内填充」，不会再看不出内容像是漏翻或渲染异常。
- 「词缀上色」词缀预览改为**按显示行**呈现：一条词缀在描述文件里常有多行文本（如「物品数量提高 {0}%」与「物品数量降低 {0}%」是同一个词缀的正反两个方向、或不同档位区间用不同文案），游戏按实际数值只显示其中一行。此前工作台把它们拼成一整段，读起来像"提高 X%降低 X%"连在一起，现在逐行显示，与游戏里的实际呈现一致；检查器里的预览同步改了。
- 「词缀上色」颜色支持透明度（RGBA）并新增「等级色阶」快捷方式：颜色修改里多了 A（透明度）滑条，「新增下一级」会以选中颜色为基础生成同色系、透明度 −50、id 末位数字 +1 的下一等级（如 `LUK1` → `LUK2`），一次把一整套等级色建好；颜色 id 现在可以直接改。带透明度的颜色按游戏支持的 `a,r,g,b` 四段写入（不透明的仍用原生 `r,g,b` 三段），随「应用词缀修改」写入 uisettings.xml 后全局可用；旧方案文件没有透明度字段，读回来按不透明处理。
- 「词缀上色」词缀列表支持勾选与批量操作：每条词缀左侧有勾选框，「全选 / 反选 / 清空勾选」只作用于当前筛选结果，「给勾选项上色」用「条目检查器」里选中的颜色一次指派给所有勾选项。搜索框新增「正则」开关，可按正则表达式匹配中文文本或 stat ID（表达式非法会提示并保留原列表）。勾选状态按词缀记录，切换方案或重建列表都不会丢。
- 「词缀上色」自动识别客户端语言、只给这一种语言上色：连接时按客户端里的语言资源判定是**简体中文**还是**繁体中文**（都没有则英文），显示文本与上色都用这一种语言段——简中客户端只改简中段，繁中客户端只改繁中段；若某条词缀没有这一段，按 简中 > 繁中 > 英文 退回，一条词缀只打一处标签，其余 7 种语言一律不动。显示文本与客户端语言不一致的条目会在列表里标出来。顺带解决两件事：只有英文的词缀、以及被语言精简包压成单语言的客户端，此前工作台读不到也上不了色，现在都能正常列出与上色。
- 「词缀上色」左侧数据源改为**分组树**：点分组看整组、点单个来源只看该来源，分组名与文件显示名都用中文，并显示各自条目数；搜索框不再被数据源选择占用（此前选中来源会把路径填进搜索框），现在可与来源筛选叠加使用。游戏更新新增的描述文件会自动归入「其他」，不会漏掉。
- 「地面燃烧特效」补丁升级到 v2（写入 `PATCHED\OilGround_v2`），新增去掉油面点燃时的过渡动画：为基础与 Abyssal / Divine 皮肤的 `transition_FIRE.ao` 各生成一份只保留骨架的独立副本 `transition_FIRE_oil*.ao`（移除火焰波粒子与 `OilIgnite` 点燃音效），并把 miscanimated.datc64 中三个点燃过渡 Id 分别重定向到各自的副本（三个 Id 共用一个目标路径会与表中相邻行的 Id 链字段冲突、被引擎判为歧义），`oilground.ot` 预加载同步指向基础副本；共享原版 `transition_FIRE.ao` 与其他技能（炸药榴弹、瓦尔守卫等）的引用完全不动。已应用 v1 的会自动识别出新增操作，重复应用不重复写入。
- 特效补丁不再要求补丁描述文件里写 `version`：没有这一项也能应用、还原，写入位置固定为 `PATCHED\<补丁名>`；写了才额外加 `_v<版本号>` 后缀（内置补丁照旧）。补丁生成器生成的补丁默认不再写版本号。
- 「特效补丁」页面重构：内置补丁由下拉菜单改为**勾选列表**，可勾选多个一起执行（启用 / 恢复 / 状态 / 卸载都会逐个应用到每个勾选的补丁）；自定义补丁一次只能选择并应用一个，与内置补丁互斥（勾了内置就停用自定义输入框，填了自定义路径就取消并停用内置勾选），右边只剩引擎输出日志。
- 补丁生成器独立为新的「创建补丁」模块（POE2）：diff 两份索引自动生成补丁的功能整体移入，特效补丁系统的整体介绍也一并放进这个模块；「特效补丁」「创建补丁」都归入 POE2 分组（选了 POE1 客户端时置灰），侧边栏补上了各自的图标。
- 内置特效补丁拆分为两个，可在“特效补丁”页面分别启用：
  - **地面燃烧特效**（`oil-ground-fx-lite`，写入 `PATCHED\OilGround_v1`）：延长黏油榴弹留下的地面燃烧的淡出；
  - **黏油榴弹特效**（`oil-grenade-fx-lite`，版本升到 v2，写入 `PATCHED\OilGrenade_v2`）：去掉落地的油花与点燃火波。
- 之前启用过原合一补丁的，两个新补丁会自动识别为已应用，重复执行不会重复写入；补丁状态判定基于内容，不受落盘位置变化影响。
- 命令行 `fx-oilmod` 支持指定补丁：`fx-oilmod <game-data> [patch-id|all] <action>`，省略补丁 ID 或用 `all` 对两个内置补丁都执行（与旧行为兼容）。

## v0.1.9 (2026-09-13)

### 新功能
- 命令行新增 `extract-file <游戏数据> <路径> <输出>`：把索引里的任意文件取出来存到磁盘，方便制作补丁和素材。
- 自定义补丁支持“整包替换型”补丁包：如果 zip 里直接带着 `_.index.bin` 与其他 `.bin`（Bundle），应用时会先把游戏目录里的同名文件备份到 `Bundles2\backup\<补丁名>\`，再逐个覆盖；点「恢复游戏原版」可按备份放回去。补丁名优先取补丁 ID，没有描述文件时取 zip 文件名。原版基线 `backup\_.index.bin` 保持其专用用途，索引内容一致时直接复用、不重复复制。
- 选择游戏数据后立即保留一份原始索引：如果客户端目录的 `backup\_.index.bin` 还不存在就自动复制一份（GGPK 客户端会从容器里取出 `_.index.bin` 再保存）。以后用其他工具改动客户端之前，还原所需的原版索引就已经在了；已存在则不会重复复制。
- 补丁生成器支持自定义补丁名字（留空仍用 `diff-年月日-时分`），并新增默认勾选的「压缩为 zip」：zip 根目录就是 `assets` 与补丁描述文件两项，可直接分发、直接应用。生成成功后自动打开补丁所在目录。

### 改进
- 主界面顶部新增「选择游戏数据」：当前客户端路径常驻显示，可随时切换，全程序共用这一份选择，各模块不再各自弹选择框，切换后立刻同步。第一次打开会提示先选客户端；在识别完客户端版本之前，当前客户端用不了的模块会先灰掉，避免点进去才发现用不了。
- 关闭程序时，如果「探索」里还有未保存的修改，会先问「保存 / 放弃 / 取消」，不再直接把改动丢掉。
- 特效补丁界面收敛：主操作只留「启用特效补丁」与「恢复游戏原版」两个按钮，各配一行说明；只读的「刷新补丁状态」和不可逆的「完全卸载补丁（连文件一起删）」收进默认折叠的「高级维护」，不再和主操作平铺。「清理孤儿补丁」按钮移除——它的活已并入「完全卸载补丁」（命令行 `cleanup` 仍在）。按钮同时改为说人话的名字，并说明各自会不会删文件。
- 特效补丁的「补丁来源」不必先改下拉菜单：直接点「自定义补丁」选文件（或把路径粘进输入框），来源会自动切到自定义补丁；想用内置补丁时再从下拉菜单切回去即可。
- 「修改地图标签」改为按需读取游戏数据：进入模块不再自动读取客户端（此前每次点进来都会占用约 1 GB 内存）。预览改用内置素材，PoE1 / PoE2 进入模块都能直接调样式；只有点「写入地图标签」时才真正打开客户端。离开模块后再回来会保留已载入的设置，不需要重复读取。
- 补丁写入位置由「执行时间」改为「补丁版本号」：同一个版本反复应用 / 还原始终复用同一个 Bundle 文件（`PATCHED\<补丁名>_v<版本号>.bundle.bin`），不再在磁盘上堆积。补丁描述新增必填的 `version` 字段，改了补丁内容必须升版本，否则新内容会被当成覆盖其他 Mod 而拒绝执行。

### 修复
- 修复作者只打包索引 + Bundle、没有描述文件的 zip 补丁包（整包替换型）无法应用的问题：此前压缩包里找不到 `patch.json` 就直接报错退出，根本没解压，自然识别不到包里的 `_.index.bin`。现在这类包会照常解压并识别，点「启用特效补丁」即可（会先把游戏目录里的同名文件备份到 `Bundles2\backup\<补丁名>\`）。
- 补充「游戏是否在运行」检测遗漏的客户端进程名（`PathOfExile_x64Steam`、`PathOfExile_KG`、`PathOfExile_x64_KG`）：这些启动方式此前检测不到，会导致「请先关闭游戏」的判断失准。各种启动方式一视同仁，与 Steam 版同样处理。
- 修复下载数据表结构（schema）时可能写坏本地文件的问题：现在限制单次下载大小（16 MiB）并在保存前校验内容，拉到异常内容（例如错误页面）会直接丢弃而不是覆盖本地 schema。
- 修复写入过程中出错会留下半截改动的问题：批量写入改为事务，中途失败会自动回滚到写入前的状态，并删掉这次新建的 Bundle，不会留下写了一半的索引。
- 修复客户端看不到新版本更新提示的问题：更新检查源由单一镜像改为 GitHub 仓库 `version.json`（经 gh-proxy 代理加速，失败时回退 GitHub raw 直连），发版后无需再维护镜像同步。
- “前往发布页”改为打开 GitHub Releases 页面。
- 修复特效补丁模块反复点「刷新补丁状态」「启用特效补丁」「生成补丁」后内存不回落的问题：这几条路径用完索引后会立即回收内存（此前只有主界面的打开/释放路径会回收），首次创建原始索引备份时同样如此。
- 修复保存与还原索引时的内存尖峰：索引校验哈希改为分块计算、还原改为文件到文件复制，不再把整份索引读进内存（此前每次保存会额外占用数百 MB）。
- 修复模块输出日志无限增长的问题：特效补丁的引擎输出区与「探索」模块的日志现在只保留最近若干行。
- 修复「完全卸载补丁（连文件一起删）」会破坏客户端的问题：此前它按前缀删除补丁 Bundle 下的**全部**文件记录，而还原补丁会把被改过的游戏原有文件（如 `data/balance/miscanimated.datc64`）的内容重定向进补丁 Bundle，那些记录是客户端读取该文件的唯一途径——删掉后文件在索引里彻底消失，客户端会加载失败。现在只移除补丁自己新增的文件，游戏原有文件一律保留；同时加了二次确认，按钮提示也改为说明真实行为。

## v0.1.7 (2026-09-11)

### 新功能
- 数据浏览器文件列表与目录树右键新增“复制为新路径（隔离副本）”，可把索引中的文件复制成一份独立副本。
- 新增 `copy-file <game-data> <源路径> <目标路径>` 命令行命令，便于脚本化和批量操作。
- 新增文件会写入新的 Bundle，并在索引中追加对应的路径记录；原始文件以及其他引用它的技能/资源完全不受影响。
- 该能力适合修改被多处复用的特效、文本等资源：先复制为专用副本，再只改副本并改写引用。

### 改进
- 物价标注不再在打开页面时自动检测联盟：不再自动打开游戏数据识别客户端版本，也不再自动拉取 poe.ninja 联盟列表，改为必须手动点击“检测当前联盟”，避免打开时占用大量内存。
- 打开页面时只回填上次使用过的联盟（纯本地配置，不访问网络、不读取游戏数据）；联盟框下方新增提示引导手动检测。
- 从物价标注切换到其他模块 **5 秒后**自动清理：丢弃联盟列表、已识别的客户端版本、日志与统计，并释放索引等占用的内存（回到该模块会按缓存重建，正在检测/标价时不会打断，会延后到任务结束）。
- 复制前自动创建原始索引备份并写入操作日志，可用 `restore` 命令或数据浏览器的恢复功能还原。
- 工具箱的所有改动统一写入单一自定义 Bundle `LibGGPK3/0.bundle.bin`；不再在超过 200 MB 上限时自动分裂出 `LibGGPK3/1`、`LibGGPK3/2` …，方便把一次修改当作单个文件分发与备份。
- 写入游戏数据后不再自动重新打开数据源（保存全部修改、替换文件、复制为新路径），改为提示点击“重新打开”，避免每次保存都要重新解析整份索引。
- 内存回收逻辑抽为独立子模块 `MemoryReclaimer`（延迟多轮回收 + 工作集裁剪），物价标注与数据浏览器释放游戏数据时统一调用；释放后立刻换文件的场景不再被“有模块占用”误判而跳过回收。
- 新增 `GameDataLoader` 子模块统一管理游戏数据的打开、路径解析与释放；物价标注的“永久繁体中文”“还原”“语言状态检测”改为后台执行，点击后不再卡住界面。
- 全部一次性打开游戏数据的调用点统一改为 `GameDataLoader.Use` / `GameDataLoader.UseAsync`：命令行 9 处、ReplaceService 4 处、CategoryPriceTagger 2 处、Poe2FontService 2 处、物价标注客户端识别 1 处；关闭后都会触发 `MemoryReclaimer` 自动回收（同步与异步路径一致）。数据浏览器的两处长生命周期句柄改为复用 `GameDataLoader.ResolvePath` 单一路径规则，不做生命周期迁移。

### 修复
- 修复释放游戏数据后内存只降一半（1.1 GB → 约 500 MB）的问题：`GC.Collect` 只回收对象，不会立刻把空闲页还给系统，且工作集不会被裁剪。现在释放流程会在数秒内补做两轮延迟回收，并调用 `SetProcessWorkingSetSize(-1,-1)` 裁剪工作集；实测同一 3.1M 文件索引从 1.13 GB 降到 177 MB（提交）/ 8 MB（工作集）。
- 修复“释放游戏数据”后进程内存不下降的问题：释放时会一并丢弃索引持有的文件记录、路径字符串和目录数据，并触发一次包含大对象堆压缩的完整垃圾回收。
- 修复文件列表在释放后仍保留目录引用，导致整份索引被判定为“仍在用”而无法回收的问题。
- 修复以只读流打开 GGPK 时 `Dispose` 不关闭底层流，造成文件句柄与缓冲区泄漏的问题。

## v0.1.6 (2026-09-11)

### 新功能
- GGPK 数据浏览器支持直接编辑文本类文件，包括 `.ao`、`.txt`、`.json`、`.xml`、`.ini`、`.csv` 和 Shader 文件。
- 编辑器支持查找下一个、区分大小写和全部替换。
- 编辑修改先暂存到内存，点击“保存全部修改”后统一写入 GGPK/INDEX；保存前自动创建原始索引备份并记录日志。
- 支持查看本次待保存文件的改动摘要，以及撤销全部尚未保存的修改。
- GGPK 数据浏览器目录树和文件列表右键增加“复制文件路径”。
- 新增术语翻译功能，支持从游戏数据构建术语词典并进行术语翻译。

### 改进
- 编辑器支持字体缩放并记住字体大小设置。
- 保存时保留 UTF-8、UTF-16、UTF-32 及 BOM 编码。
- 翻译功能使用内置术语资源，并支持可取消的后台翻译任务。

## v0.1.5 (2026-09-08)

### 新功能
- 更新提示改为中央更新页，显示新版本中文更新日志；支持跳过当前版本或前往发布页。
- 新增“打开数据文件夹”入口，可直接查看工具的配置、备份和缓存文件。

### 改进
- 配置、游戏数据备份、PoB 补丁备份和物价缓存统一保存至 `%LOCALAPPDATA%\PoEToolbox`，旧备份会在下次使用时迁移。
- 首次使用说明增加每日自动检测更新的提示。
- 暂时隐藏多语言切换入口，界面固定使用简体中文。

## v0.1.4 (2026-09-08)

### 新功能
- 新增自动更新检测，启动后后台检查远程 `version.json`，每 24 小时最多请求一次。
- 版本号区域改为可点击入口，发现新版本时显示红点提示。

### 改进
- 联盟检测优先识别当前游戏数据文件是 PoE1 还是 PoE2，避免将 PoE1 联盟和分类错误套用到 PoE2 客户端。
- 游戏数据相关界面统一使用 `Content.ggpk / _.index.bin` 提示；检测按钮明确为“检测当前联盟”，并显示当前识别的游戏版本。
- 调整物价标注布局，扩大设置和输出区域，收紧分类区域；隐藏详细日志选项。

### 修复
- 修复关闭主窗口后可能残留后台进程的问题。

## v0.1.3 (2026-09-08)

### 修复
- 修复 POE2 独立客户端 `Content.ggpk` 包含少量无法解析索引路径时，读取客户端数据失败的问题。

## v0.1.2 (2026-09-08)

### 新功能
- 新增 POE2 字体配置器，支持选择 `Content.ggpk` 或 Bundles2 `_.index.bin`。
- 字体列表读取系统已安装的全部字体，也支持手动输入字体族名称。
- 支持统一调整字号倍率，并在界面右侧实时预览中文、英文、数字和不同字号效果。

### 改进
- POE2 字体配置基于首次备份的原始 XML 生成，重复调整不会累乘字号。
- 更新 `metadata/ui/uisettings.xml` 的字体、字号和 CJK/Any fallback，并生成继承基础配置的繁体中文 XML。
- 保留 XML 编码和中文配置注释，支持恢复首次应用前的原始字体文件。

## v0.1.1 (2026-09-08)

### 改进
- 完成可靠性审计整改：行情获取返回结构化状态，失败或部分成功时停止标价，避免将旧缓存误当作最新数据。
- GGPK、Bundles2、PoB 补丁和文件导出增加临时文件替换、失败回滚与基线恢复保护。
- 配置、日志、DAT schema 和数据浏览器缓存统一使用用户数据目录，并补充发布/兼容矩阵说明。
- 背包清理和 Voyager 增加前台窗口校验、输入失败中止、取消处理和修饰键清理。

### 修复
- 移除攻略翻译模块及其项目引用、资源和历史文案。
- 修复单文件、批量目录和 DAT JSON 导出在取消或写入失败时留下半文件的问题。
- 修复 Voyager 坐标无效时取消源未释放以及停止后仍报告完成的问题。
- 新增行情解析、PoB 补丁回滚、DAT 扩展识别、路径安全和原子写入回归测试。

## v0.1.0 (2026-09-07)

### 新功能
- 数据浏览器支持将 `.dat`、`.dat64`、`.datc64` 和 `.datcl64` 文件提取为 JSON。
- DATC64 支持 `interval` 字段，导出为 `[min, max]`，空引用导出为 `null`。
- Shell 增加共享游戏上下文和联盟状态，底部状态栏显示客户端、运行状态与当前联盟。

### 改进
- Bundles2 索引使用只读 mmap 路径，批量导出按 Bundle 可控并行并降低进度通知频率。
- DATC64 JSON 导出改为流式写入，减少大表导出的内存占用，并保留 schema 未描述的尾部字节。
- PoE2 的“繁体中文补丁”按钮改为显示但禁用，分类 Chip 按内容自适应宽度，避免“未切割的宝石”等文本被裁切。
- 统一版本号为 `0.1.0`。

## v0.0.9 (2026-09-07)

### 新功能
- 地图标签支持 PoE2：批量修改 `endgamemap1.dds` 至 `endgamemap15.dds`，并同步重建低 mip。
- PoE2 地图标签使用 PoE1 圆形底图，在中心写入阿拉伯数字 1 至 15，移除原素材内嵌的罗马数字。

### 改进
- 地图标签移至通用工具分组，自动识别 PoE1 / PoE2 客户端并选择对应资源。

### 修复
- 数据浏览器打开 PoE2 Bundles2 索引时跳过无法解析路径的记录，避免构建目录树失败。
- 修复切换页面或取消操作时提前释放取消源导致的打开失败。

## v0.0.8 (2026-09-06)

### 新功能
- 物价标注支持 PoE1 与 PoE2 联盟列表，并允许手动输入自定义联盟
- 自动识别 PoE1 / PoE2 客户端；PoE2 支持 Steam Bundles2 数据文件
- PoE1 与 PoE2 基础物品中英名称词典内置到发布文件，首次标价无需在用户端生成词典
- 地图标签调整为独立的 PoE1 插件，可单独打开并修改地图数字

### 改进
- 多分类标价改为单次打开客户端、备份和写入，减少处理时间与文件占用
- PoE2 分类显示改为中文名称
- 插件导航按 PoE1、PoE2 和通用工具分组；PoB 国服补丁固定为 PoE1

### 修复
- 修复联盟下拉列表选择后文本被清空的问题
- 修复 PoE2 未切割宝石、探险通货等级、重音名称、碎片别名与短通货 ID 的价格映射

## v0.0.7 (2026-08-06)

### 新功能
- 新增 PoB 国服补丁模块：支持选择 PoB 目录、识别 PoE1 / PoE2，并可输入赛季编号后应用国服交易站补丁或还原国际服
- PoB 国服补丁页面新增 PoB 与 PoB2 最新便携版下载入口
- 地图标签支持新版 DX10 DDS 格式，并在写入后重建全部 mip 级别

### 改进
- 主窗口及 PoB 补丁页面扩大布局，目录选择控件完整显示
- 物价标注模块改名为「物价标注(国际服)」
- 切离数据浏览模块、完成地图标签写入后自动释放 GGPK 文件占用

### 修复
- 修复 PoB 国服补丁预览在界面初始化时触发的空引用异常
- 修复地图数字预览无法读取 DX10 DDS 的问题
- 暂时隐藏地图标签颜色编辑与同步颜色控件，避免显示结果与预期颜色不一致

## v0.0.6 (2026-08-05)

### 新功能
- GGPK 数据浏览器支持批量修改 `mapnumbers1.dds` 到 `mapnumbers16.dds`
- 地图标签修改支持字体选择、字号和 X/Y 偏移，颜色固定为白色、黄色、红色分组
- 地图数字弹窗使用 `mapBackground.png` 进行实时叠加预览
- 打开 GGPK 或 `_.index.bin` 后自动创建带 SHA-256 标识的索引快照，地图标签修改可恢复到打开时状态
- 支持 DDS 文本替换和地图数字批处理 CLI 命令

### 改进
- 地图数字预览背景资源嵌入单文件发布产物

## v0.05 (2026-07-31)

### 用户可见更新
- 统一工具箱版本标识为 `v0.05`
- 物价标注功能继续以 PoE1 为主
- CLI 增加 PoE2 行情数据获取入口
- 修复发布版本显示和部分数据文件读取提示问题
- GGPK 数据浏览器支持目录树、文件列表和全局搜索
- 支持预览文本、JSON、十六进制以及 DAT 数据表
- 支持提取单个文件或整个文件夹，并显示进度、失败结果和取消状态
- 支持按常用文件类型筛选搜索结果
- 数据浏览器缓存目录结构，重复打开同一份游戏数据时加载更快

## v0.0.5 (2026-07-30)

### 新功能
- **GGPK文件浏览**：像资源管理器一样浏览游戏数据文件
  - 支持 GGPK 容器和 Bundles2 目录两种格式自动识别
  - 左侧目录树 + 右侧文件列表，文件夹优先显示
  - 全局搜索：输入文件名即可从百万文件中快速定位
  - 右键提取单个文件或整个文件夹到本地
  - 显示文件大小、完整路径
- **Steam/Epic 客户端支持**：物价标注现在支持所有客户端版本
  - 官方版 (Content.ggpk)、平台版 (Bundles2/_.index.bin) 自动检测

### 改进
- 物价标注 GGPK 路径检测覆盖 Steam/Epic 客户端
- 背包清理触发键显示修饰键前缀（如 Ctrl+F2）
- UI 配色统一：所有插件按钮样式一致

## v0.0.4 (2026-07-29)

### 新功能
- **背包清理**：一键清包，自动将背包物品快速存入仓库
  - 支持自定义热键
  - 坐标校准向导，适配不同分辨率
  - 仓库筛选：可标记不清理的格子
  - 反检测：点击间隔和位置随机抖动
  - 所有设置改动自动保存
  - 内置使用说明

### 改进
- **流放工具箱统一入口**：物价标注和背包清理合并为一个应用，左侧导航切换
- **启动更快**：GGPK 路径自动缓存，切换标签不需重选
- **联盟检测更快**：15 分钟内不重复联网
- **内存自动释放**：应用完毕后回收内存
- **双击即可运行**：不再需要额外的 oo2core.dll 文件
- **管理员权限自动请求**
- **免责声明**：嵌入主窗口，不再弹出独立窗口

### 界面
- 分类按钮统一宽度，勾选前后大小不变
- 背包清理添加使用说明卡片
- 免责声明字体加大，无重复标题，支持滚动
- 主窗口尺寸加大，适配左侧导航栏
- 分类按钮文字「取消」→「取消全选」
- 紧急停止功能暂时隐藏（后续完善）
- 预览模式暂时隐藏

### 安全
- 退出时彻底清理热键和进程

### 修复
- 修复切换标签时加载卡顿
- 修复多处崩溃（切换标签、保存设置、免责弹窗等）
- 修复仓库筛选逻辑反转
- 修复退出后进程残留、oo2core.dll 占用
- 修复像素偏移默认值（±5px→±3px）
- 修复分类按钮选中后悬停变色

---

## v0.0.3 (2026-07-28)

### 改进
- 物标注确性大幅提升，不再出现物品消失或游戏崩溃
- 启动时自动检测 GGPK 文件位置和当前联盟

### 修复
- 修复游戏崩溃（磨刀石等物品消失）
- 修复多次备份问题

---

## v0.0.2 (2026-07-27)

### 新功能
- 首次使用弹窗说明
- 支持简体中文、繁体中文、英文界面切换
- 支持暗色和亮色主题

---

## v0.0.1 (2026-07-26)

### 首次发布
- 一键给物品名称追加价格标签（从 poe.ninja 获取物价）
- 支持 17 个通货分类
- GGPK 备份和一键还原
- 一键应用永久繁体中文
