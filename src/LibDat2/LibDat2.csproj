<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <NoWarn>1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- 内嵌一份：运行时不再依赖 exe 旁是否有 DatDefinitions.json（单文件发布后没有） -->
    <EmbeddedResource Include="DatDefinitions.json" LogicalName="LibDat2.DatDefinitions.json" />
  </ItemGroup>

</Project>