<Window x:Class="PoEToolbox.Plugins.BagCleaner.Views.CalibrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PoEToolbox.Plugins.BagCleaner.ViewModels"
        Title="坐标校准"
        Width="380" Height="280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Topmost="True"
        Background="{StaticResource BgBase}"
        FontFamily="Segoe UI Variable, Segoe UI, Microsoft YaHei UI"
        FontSize="13"
        Foreground="{StaticResource TextPrimary}">
    <Border Style="{StaticResource Card}" Margin="12">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center"/>
            <Border Background="White" BorderBrush="{StaticResource Border}" BorderThickness="1"
                    CornerRadius="20" Padding="14,4" HorizontalAlignment="Center" Margin="0,8,0,16">
                <TextBlock Text="{Binding Step, StringFormat='步骤 {0}'}"
                           FontSize="12" FontWeight="SemiBold">
                    <TextBlock.Foreground>
                        <SolidColorBrush Color="{Binding StepColor}"/>
                    </TextBlock.Foreground>
                </TextBlock>
            </Border>
            <TextBlock Text="{Binding Instruction}"
                       FontSize="13"
                       Foreground="{StaticResource TextSecondary}"
                       TextAlignment="Center"
                       LineHeight="22"
                       TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</Window>
